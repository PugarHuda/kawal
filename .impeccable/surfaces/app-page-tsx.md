---
version: 1
slug: "app-page-tsx"
primary_target: "app/page.tsx"
related_targets: []
---

# Surface: / (cover sheet, Form K-1)

Mode: Persuade. The visitor is a capital holder on BSC deciding whether to trust this catalogue at all; success is that they understand within seconds that Kawal called the agents itself, and open the manifest.

Audience and job: read fast, distrust the registry, want the receipt before the button.

Action: BROWSE THE MANIFEST (tear-off counterfoil). Secondary: see the limits (mandate), read the evidence (advantage report).

Proof on this surface: Kawal's own probe count under a violet TELAH DIPERIKSA stamp whose ink density tracks the count; the registry's three figures typed into cells and labelled as the registry's; the four seats as numbered manifest lines.

Constraints: dynamic render for the CSP nonce; no eyebrow above the H1 (the field caption lives inside the cell as a form label); the headline "Most agents on BSC cannot be hired." and the three link names are pinned by the suite.

Direction: Surat Jalan (seed dc528c41). Memorable moment: the stamp pressing over the probe count on load.

Unresolved: none.
